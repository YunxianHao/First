#include<stdio.h>
int fib(int n)
{
	int arr[100];
	if (n <= 0)
		return 0;
	else if (n == 1)
		return 1;
	else
		return fib(n - 1) + fib(n - 2);
}
int main()
{
	int n;
	printf("Please input the n\n");
	scanf_s("%d", &n);
	printf("%d", fib(n));
	return 0;
}
